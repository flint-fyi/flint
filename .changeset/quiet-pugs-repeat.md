---
"@flint.fyi/core": minor
"@flint.fyi/rule-tester": patch
---

Add `withRepositoryRoot` to compose a known repository root onto a host, and use it so disk-backed rule testers resolve `getRepositoryRoot()`.
