<section class="try-it-out not-content" id="try-it-out">
	<div class="content">
		<h2 class="flint-display">Try Flint on any project</h2>

		<p class="aside">
			Frustrated with slow linting that doesn't catch what you care about? Or
			maybe your linter is fast, but doesn't lint everything you think it
			should? Give Flint a shot.
		</p>

		<div
			aria-label="Representative Flint CLI output"
			class="terminal"
			role="region"
		>
			<div aria-hidden="true" class="terminal-bar">
				<span></span>
				<span></span>
				<span></span>
			</div>
			<div class="terminal-output">
				<div class="terminal-line command-line">
					<span class="prompt">$</span>
					<span>flint</span>
				</div>
				<div class="terminal-line muted">
					Linting with <span class="config">flint.config.ts</span>...
				</div>
				<div class="terminal-line file">fixtures/src/with-issues.ts</div>
				<div class="report">
					<span class="location">2:2</span>
					<span class="message">
						Debugger statements should not be used in production code.
					</span>
					<span class="rule">ts/debuggerStatements</span>
				</div>
				<div class="terminal-line summary">
					✖ Found <strong>1 report</strong> across <strong>1 file</strong>.
				</div>
			</div>
		</div>

		<a class="button" href="/about">Get started →</a>
	</div>
</section>

<style>
	.try-it-out {
		border-block: 1px solid var(--flint-paper-border);
		margin-block: 1rem 4rem;
		padding-block: clamp(3rem, 6vw, 4.5rem);
	}

	.content {
		display: grid;
		grid-template-areas:
			"heading"
			"aside"
			"terminal"
			"button";
		text-align: start;
	}

	h2 {
		color: var(--flint-band-text);
		font-size: clamp(1.875rem, calc(1rem + 2.5vw), 2.75rem);
		grid-area: heading;
		line-height: 1.15;
		margin: 0;
	}

	.aside {
		color: var(--flint-band-muted);
		font-size: var(--sl-text-lg);
		grid-area: aside;
		line-height: 1.6;
		margin: 1rem 0 0;
		max-width: 42rem;
	}

	.terminal {
		background: #071220;
		border: 1px solid #3b4d68;
		border-radius: 0.5rem;
		color: var(--flint-cream);
		grid-area: terminal;
		margin-block-start: 1.75rem;
		min-inline-size: 0;
		overflow: hidden;
		text-align: start;
	}

	.terminal-bar {
		align-items: center;
		background: #0d1d31;
		border-block-end: 1px solid #2c3e57;
		display: flex;
		gap: 0.4rem;
		min-block-size: 2rem;
		padding-inline: 0.85rem;
	}

	.terminal-bar span {
		background: #b0a08b;
		block-size: 0.55rem;
		border-radius: 50%;
		inline-size: 0.55rem;
	}

	.terminal-bar span:first-child {
		background: #f58a3a;
	}

	.terminal-bar span:last-child {
		background: #79ad6c;
	}

	.terminal-output {
		font-family: var(--sl-font-mono);
		font-size: clamp(0.72rem, 1.5vw, var(--sl-text-sm));
		line-height: 1.6;
		padding: clamp(1rem, 3vw, 1.5rem);
	}

	.terminal-line + .terminal-line,
	.terminal-line + .report,
	.report + .terminal-line {
		margin-block-start: 0.75rem;
	}

	.command-line {
		display: flex;
		gap: 0.65rem;
	}

	.prompt,
	.config {
		color: #8bd2ff;
	}

	.config,
	.summary strong {
		font-weight: 800;
	}

	.muted,
	.location {
		color: #b9c4d6;
	}

	.file {
		color: #a4dd98;
		overflow-wrap: anywhere;
		text-decoration: underline;
		text-underline-offset: 0.2em;
	}

	.report {
		display: grid;
		gap: 0.3rem 0.75rem;
		grid-template-columns: 2.75rem minmax(0, 1fr);
	}

	.rule {
		color: #ffc48a;
		grid-column: 2;
		overflow-wrap: anywhere;
	}

	.summary {
		color: #ffb48e;
	}

	@media (width >= 40rem) {
		.report {
			grid-template-columns: 2.75rem minmax(0, 1fr) auto;
		}

		.rule {
			grid-column: auto;
		}
	}

	.button {
		background: var(--flint-ember-fill);
		border: 1px solid var(--flint-ember-fill);
		border-radius: 0.5rem;
		color: var(--flint-ember-fill-text);
		display: inline-block;
		font-weight: 800;
		grid-area: button;
		justify-self: start;
		margin-block-start: 2rem;
		padding: 0.8rem 1.6rem;
		text-decoration: none;
		transition:
			background-color 150ms,
			border-color 150ms;
	}

	.button:hover {
		background: color-mix(
			in srgb,
			var(--flint-ember-fill) 88%,
			var(--flint-band-text)
		);
		border-color: transparent;
	}

	@media (width >= 50rem) {
		.content {
			column-gap: clamp(2.5rem, 5vw, 4rem);
			grid-template-areas:
				"heading terminal"
				"aside terminal"
				"button terminal";
			grid-template-columns: minmax(0, 0.85fr) minmax(0, 1.15fr);
		}

		.terminal {
			align-self: center;
			margin-block-start: 0;
		}
	}
</style>
