---
import DefaultPageSidebar from "@astrojs/starlight/components/PageSidebar.astro";

const isPlayground = Astro.locals.starlightRoute.entry.id === "playground";
---

{!isPlayground && <DefaultPageSidebar />}
