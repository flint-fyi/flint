---
description: "Reports numeric quantifiers like '{n,n}' that can be simplified to '{n}'."
title: "regexUnnecessaryNumericQuantifiers"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="ts" rule="regexUnnecessaryNumericQuantifiers" />

Reports numeric quantifiers like `{n,n}` where the minimum and maximum are the same, which can be simplified to `{n}`.

## Examples

### Simple Cases

<Tabs>
<TabItem label="❌ Incorrect">

```ts
const pattern = /a{1,1}/;
```

</TabItem>
<TabItem label="✅ Correct">

```ts
const pattern = /a{1}/;
```

</TabItem>
</Tabs>

### Larger Numbers

<Tabs>
<TabItem label="❌ Incorrect">

```ts
const pattern = /a{42,42}/;
```

</TabItem>
<TabItem label="✅ Correct">

```ts
const pattern = /a{42}/;
```

</TabItem>
</Tabs>

### Lazy Quantifiers

<Tabs>
<TabItem label="❌ Incorrect">

```ts
const pattern = /a{1,1}?/;
```

</TabItem>
<TabItem label="✅ Correct">

```ts
const pattern = /a{1}?/;
```

</TabItem>
</Tabs>

### Groups

<Tabs>
<TabItem label="❌ Incorrect">

```ts
const pattern = /(?:ab){2,2}/;
```

</TabItem>
<TabItem label="✅ Correct">

```ts
const pattern = /(?:ab){2}/;
```

</TabItem>
</Tabs>

## Options

This rule is not configurable.

## When Not To Use It

This rule is safe to use.

## Further Reading

- [MDN: Quantifier](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_expressions/Quantifiers)
- [MDN: Regular expressions](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_expressions)

## Equivalents in Other Linters

<RuleEquivalents pluginId="ts" ruleId="regexUnnecessaryNumericQuantifiers" />
