---
description: "Disallows non-null assertions."
title: "nonNullAssertions"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="ts" rule="nonNullAssertions" />

The non-null assertion operator (`!`) tells TypeScript to trust that a value is not `null` or `undefined`.
This bypasses TypeScript's strict null checking and can mask potential bugs when the value actually is `null` or `undefined` at runtime.

Prefer type guards, nullish coalescing (`??`), or optional chaining (`?.`) instead.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```ts
declare const value: string | null;

value!.toUpperCase();
```

```ts
declare const object: { property: string } | null;

object!.property;
```

```ts
declare const callable: (() => void) | null;

callable!();
```

</TabItem>
<TabItem label="✅ Correct">

```ts
declare const value: string | null;

value?.toUpperCase();
```

```ts
declare const object: { property: string } | null;

object?.property;
```

```ts
declare const value: string | null;

if (value !== null) {
	value.toUpperCase();
}
```

```ts
declare const value: string | undefined;

const result = value ?? "default";
```

</TabItem>
</Tabs>

## Options

This rule is not configurable.

## When Not To Use It

If your project is still onboarding to TypeScript's `strictNullChecks` compiler option, or more generally has unusual needs for null values, you might not receive false reports from this rule.
You might consider using [Flint disable comments](/directives) and/or [configuration file disables](/configuration#files) for those specific situations instead of completely disabling this rule.

## Further Reading

- [TypeScript: Non-null assertion operator](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#non-null-assertion-operator-postfix-)
- [MDN: Nullish coalescing operator (??)](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Nullish_coalescing)
- [MDN: Optional chaining (?.)](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Optional_chaining)

## Equivalents in Other Linters

<RuleEquivalents pluginId="ts" ruleId="nonNullAssertions" />
