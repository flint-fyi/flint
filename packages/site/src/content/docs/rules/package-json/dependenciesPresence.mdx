---
description: "Enforce that the `dependencies` property is present."
title: "dependenciesPresence"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="dependenciesPresence" />

npm requires that the `dependencies` field in `package.json` is present.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{}
```

```json
{
	"name": "my-repository"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
	"dependencies": {
		"flint": "*"
	}
}
```

```json
{
	"dependencies": {
		"flint": "*"
	},
	"name": "my-repository"
}
```

</TabItem>
</Tabs>

## When Not To Use It

If your package doesn't need to specify `dependencies`, you can disable this rule.

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="dependenciesPresence" />
