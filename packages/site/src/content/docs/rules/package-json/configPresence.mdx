---
description: "Enforce that the `config` property is present."
title: "configPresence"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="configPresence" />

npm requires that the `config` field in `package.json` is present.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{}
```

```json
{
	"name": "my-repository"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
	"config": { "port": 8080 }
}
```

```json
{
	"config": { "port": 8080 },
	"name": "my-repository"
}
```

</TabItem>
</Tabs>

## When Not To Use It

If your package doesn't need to specify `config`, you can disable this rule.

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="configPresence" />
