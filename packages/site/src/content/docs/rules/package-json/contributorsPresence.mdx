---
description: "Enforce that the `contributors` property is present."
title: "contributorsPresence"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="contributorsPresence" />

npm requires that the `contributors` field in `package.json` is present.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{}
```

```json
{
	"name": "my-repository"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
	"contributors": [{ "name": "First Last" }]
}
```

```json
{
	"contributors": [{ "name": "First Last" }],
	"name": "my-repository"
}
```

</TabItem>
</Tabs>

## When Not To Use It

If your package doesn't need to specify `contributors`, you can disable this rule.
For example, if your package is local-only and is never published online or made open source, contributor attribution may not be relevant for you.

## Related Rules

- [contributorsValidity](/rules/ts/contributorsvalidity) - Enforces that the `contributors` property is valid.

## Further Reading

- [npm: `package.json` > `people fields: author, contributors`](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#people-fields-author-contributors)

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="contributorsPresence" />
