---
description: "Enforce that the `directories` property is present."
title: "directoriesPresence"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="directoriesPresence" />

npm requires that the `directories` field in `package.json` is present.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{}
```

```json
{
	"name": "my-repository"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
	"directories": {
		"bin": "dist/bin"
	}
}
```

```json
{
	"directories": {
		"bin": "dist/bin"
	},
	"name": "my-repository"
}
```

</TabItem>
</Tabs>

## When Not To Use It

If your package doesn't need to specify `directories`, you can disable this rule.

## Related Rules

- [directoriesValidity](/rules/ts/directoriesvalidity) - Enforces that the `directories` property is valid.

## Further Reading

- [npm: `package.json` > `directories`](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#directories)

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="directoriesPresence" />
