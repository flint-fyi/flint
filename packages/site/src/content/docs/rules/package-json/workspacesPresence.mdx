---
description: "Enforce that the `workspaces` property is present."
title: "workspacesPresence"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="workspacesPresence" />

npm requires that the `workspaces` field in `package.json` is present.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{}
```

```json
{
	"name": "my-repository"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
	"workspaces": ["packages/a"]
}
```

```json
{
	"name": "my-repository",
	"workspaces": ["packages/a"]
}
```

</TabItem>
</Tabs>

## When Not To Use It

If your package doesn't need to specify workspaces, you can disable this rule.

## Related Rules

- [workspacesValidity](/rules/package-json/workspacesvalidity) - Enforces that the `workspaces` property is valid.

## Further Reading

- [npm: `package.json` > `workspaces`](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#workspaces)

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="workspacesPresence" />
