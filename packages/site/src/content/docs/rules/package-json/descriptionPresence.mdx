---
description: "Enforce that the `description` property is present."
title: "descriptionPresence"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="descriptionPresence" />

npm requires that the `description` field in `package.json` is present.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{}
```

```json
{
	"name": "my-repository"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
	"description": "Hello, world! ✨"
}
```

```json
{
	"description": "Hello, world! ✨",
	"name": "my-repository"
}
```

</TabItem>
</Tabs>

## When Not To Use It

If your package doesn't need to specify a `description`, you can disable this rule.
For example, if your package is local-only and is never published online or made open source, adding a description may not be relevant for you.

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="descriptionPresence" />
