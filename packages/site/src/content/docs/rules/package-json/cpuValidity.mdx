---
description: "Enforce that the `cpu` property is valid."
title: "cpuValidity"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="cpuValidity" />

The rule checks that, if present, the `cpu` property is an array of non-empty strings.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{
	"cpu": true
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
	"cpu": ["x64", "ia32"]
}
```

</TabItem>
</Tabs>

## When Not To Use It

If you don't restrict CPU architectures in your projects, you can disable this rule.

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="cpuValidity" />
