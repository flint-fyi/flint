---
description: "Enforce that the `publishConfig` property is present."
title: "publishConfigPresence"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="publishConfigPresence" />

npm requires that the `publishConfig` field in `package.json` is present.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{}
```

```json
{
	"name": "my-repository"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{}
```

```json
{
	"name": "my-repository"
}
```

</TabItem>
</Tabs>

## When Not To Use It

If your package doesn't need to specify a `publishConfig`, you can disable this rule.

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="publishConfigPresence" />
