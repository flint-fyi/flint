---
description: "Enforce that the `peerDependencies` property is valid."
title: "peerDependenciesValidity"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="peerDependenciesValidity" />

The rule checks that, if present, the `peerDependencies` property is an object where each key is a valid package name and each value is a valid version range.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{
	"peerDependencies": {
		"invalid-version": "catalob:"
	}
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
	"peerDependencies": {
		"nin": "catalog:",
		"thee-silver-mt-zion": "^1.2.3",
		"david-bowie": "*"
	}
}
```

</TabItem>
</Tabs>

## When Not To Use It

If you don't validate dependency versions in your projects, you can disable this rule.

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="peerDependenciesValidity" />
